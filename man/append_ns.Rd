% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/append_ns.R
\name{append_ns}
\alias{append_ns}
\title{Tastefully combine "label" and "n" columns}
\usage{
append_ns(dataset, append_to = label, by_group_var = FALSE, newline = FALSE)
}
\arguments{
\item{dataset}{A dataframe}

\item{append_to}{(default: label). The variable to which you wish to add (n = ...)}

\item{by_group_var}{(default: FALSE). If FALSE, takes ns by row. If TRUE, takes group ns by group_var levels. Set to TRUE if making a stacked bar chart}

\item{newline}{(default: FALSE). If FALSE, (n = ...) comes after a space " ". If TRUE, it comes after a hard return}
}
\description{
Uses the output from the y2clerk freqs() function and adds (n = ...) to the end of each label, based off the column "n". Useful for charts that require ns for all levels
}
\examples{

# by_group_var = FALSE
ToothGrowth \%>\%
  freqs(supp) \%>\%
  append_ns()

ToothGrowth \%>\%
  dplyr::group_by(supp) \%>\%
  freqs(dose) \%>\%
  append_ns(newline = TRUE)

# by_group_var = TRUE
ToothGrowth \%>\%
  dplyr::group_by(supp) \%>\%
  freqs(dose) \%>\%
  append_ns(
    append_to = group_var,
    by_group_var = TRUE
  )
}
